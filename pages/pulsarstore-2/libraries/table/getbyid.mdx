import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# GetByID
<Method
	name="PulsarStore.Table.GetByID"
	realm="shared"
	args={[
		<Argument type="table" name="table">The table to look in</Argument>,
		<Argument type="number" name="id">The ID to look for</Argument>
	]}
	returns={[
		<ReturnValue type="table">The table that contains the ID</ReturnValue>
	]}
>
	Gets a table from a given ID. This is useful for getting a table from a given ID which is inside a larger table.
</Method>