import MethodList from "../../../components/method_list";

## Table Methods
<MethodList methods={[
	{
		name: "KeyFromID",
		parent: "PulsarStore.Table.",
		description: "Returns the key for a given ID in a table. This only checks values in a table which are `id`",
		args: ["table, id"],
		returns: ["key"],
	},
	{
		name: "GetByID",
		parent: "PulsarStore.Table.",
		description: "Gets a value from a table by ID. This only checks values in a table which are `id`",
		args: ["table, id"],
		returns: ["table"],
	},
	{
		name: "RemoveByID",
		parent: "PulsarStore.Table.",
		description: "Removes a value from a table by ID. This only checks values in a table which are `id`",
		args: ["table, id"],
		returns: [""],
	},
	{
		name: "UpdateByID",
		parent: "PulsarStore.Table.",
		description: "Updates a value in a table by ID. This only checks values in a table which are `id`",
		args: ["table, id"],
		returns: [""],
	}
]} />