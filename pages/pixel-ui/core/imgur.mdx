# Imgur ![](http://localhost:3000/client.png)

##### `PIXEL.GetImgur(id, callback, useproxy, matSettings)`
  Gets and downloads an imgur image, if useproxy is enabled then it will proxy it through DuckDuckGo

---
