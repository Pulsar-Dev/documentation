import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# KeyFromID
<Method
	name="PulsarStore.Table.KeyFromID"
	realm="shared"
	args={[
		<Argument type="table" name="table">The table to look in</Argument>

		<Argument type="number" name="id">The ID to look for</Argument>
	]}
	returns={[
		<ReturnValue type="number">A key inside a table</ReturnValue>
	]}
>
	Gets a parent tables key from a given ID. This is useful for getting the key of a table from a given ID which is inside a child table.
</Method>