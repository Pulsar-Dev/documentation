---
id: partial
title: partial
---

import Method from '../../../components/method';
import Argument from '../../../components/method/argument';
import ReturnValue from "../../../components/method/return_value";

# partial
<Method
    name="PulsarLib.Functional.partial"
    realm="shared"
    args={[
        <Argument type="function" name="func">Input function to curry</Argument>,
        <Argument type="any" name="...">Arguments to store.</Argument>,
    ]}
    returns={[
        <ReturnValue type="function" name="function">Partial function</ReturnValue>
    ]}
    repositoryUrl="https://github.com/Pulsar-Dev/pulsar-lib"
    declaration="lua/pulsar_lib/core/sh_functional.lua#L13-L25"
>
	Builds a partial function, with stored arguments.
</Method>

## Example

```lua
local positiveOnly = partial(max, 0)
print(positiveOnly(-1)) -- Returns 0.
print(positiveOnly(2)) -- Returns 2.
```